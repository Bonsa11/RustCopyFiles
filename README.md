# RustCopyFiles
---

```
CSV with two columns src and dst
foo.txt    bar.txt

would copy the file foo.txt to bar.txt
CSV will open with NO HEADERS, so may raise a file not found error if run with headers

```
